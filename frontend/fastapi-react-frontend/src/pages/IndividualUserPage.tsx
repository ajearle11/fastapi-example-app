const IndividualUserPage = () => {
  return (
    <div>IndividualUserPage. This is where I would probably create another endpoint for getUserById and then use the id from the params to get all their data. As it is out of scope of the task I thought I would just leave it here.</div>
  )
}

export default IndividualUserPage